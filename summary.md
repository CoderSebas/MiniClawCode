# Project Summary

## README.md
**MiniClawCode** — A lightweight AI coding assistant built from scratch, inspired by learn-claude-code.

## requirements.txt
The project depends on the following Python packages:
- `anthropic` (>= 0.25.0) — likely used to interface with Anthropic's Claude API
- `python-dotenv` (>= 1.0.0) — for loading environment variables from a `.env` file
- `pyyaml` (>= 6.0) — for parsing YAML configuration files
